// src/settings/player_settings.rs

#![enable(implicit_some)]
(
    components: [
        Size((
            w: 32.0,
            h: 32.0,
        )),
        Hitbox(Size),
        Collider(Player),
        Collidable(Player),
        Solid(Player),
        Velocity((x: 0.0, y: -100.0)),
        OnLane((
            current: 0,
            switch_speed: 500.0,
        ))
    ],
)
